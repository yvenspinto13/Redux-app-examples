import { SidebarWatchDirective } from './sidebar-watch.directive';

describe('SidebarWatchDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarWatchDirective();
    expect(directive).toBeTruthy();
  });
});
